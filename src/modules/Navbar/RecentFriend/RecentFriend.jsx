/*
import React from "react";
import classes from './RecentFriend.module.css'

const RecentFriend = (props) => {
    return(
      <div className={classes.item}>
          {props.name}
      </div>
    );
}

export default RecentFriend*/

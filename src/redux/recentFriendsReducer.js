
let initialState = [
    {id: 1, name: 'Andrew'},
    {id: 2, name: 'Lexa'},
    {id: 3, name: 'Dimas'}
];

const recentFriendsReducer = (state = initialState, action) => {


    return state;
};

export default recentFriendsReducer;